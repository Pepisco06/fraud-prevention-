import UserInfoDetailed from "./UserInfoDetailed";

const page = () => {
  return (
    <section className="flex gap-12 justify-center">
      <UserInfoDetailed />
    </section>
  );
};

export default page;
